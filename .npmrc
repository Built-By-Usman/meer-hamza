# Enforce generating package-lock.json
package-lock=true

# Pin packages to exact versions by default (no caret/tilde auto-upgrades)
save-exact=true

# Enforce security audits during installs
audit=true
